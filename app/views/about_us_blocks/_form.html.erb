<%= form_for(@about_us_block) do |f| %>
  <% if @about_us_block.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@about_us_block.errors.count, "error") %> prohibited this about_us_block from being saved:</h2>

      <ul>
      <% @about_us_block.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <h2>Заголовок:</h2><br>
    <%= f.text_field :header %>
  </div>
  <div class="field">
    <h2>Тело:</h2><br>
    <%= f.cktext_area :body, rows: 50 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
